import type { Context } from '@deepseek-ai/cordis'

/** Stable's upstream array menu has no menu-item slot; the runtime patch supplies it. */
export function installSessionWindowMenu(_ctx: Context): void {}
